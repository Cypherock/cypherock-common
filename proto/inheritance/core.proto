syntax = "proto3";

import "error.proto";
import "inheritance/wallet_auth.proto";

package inheritance;


message Query {
    oneof request{
        WalletAuthRequest wallet_auth = 1;
    }
}

message Result {
    oneof response {
        error.CommonError common_error = 1;
        WalletAuthResponse wallet_auth = 2;
    }
}